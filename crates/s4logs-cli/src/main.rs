//! s4logs — CLI entrypoint. Wave 2D implements subcommands
//! (drain / grep / restore / serve) per DESIGN.md §9.

fn main() {
    eprintln!("s4logs: P1 wave 2 not yet wired (see DESIGN.md)");
    std::process::exit(2);
}
